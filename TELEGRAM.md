# بوت تليقرام

يتيح محادثة النظام من تليقرام: تسأل بالعربية الطبيعية عن المستفيدين والمرافقين
والأصناف وقائمة الطعام وأوامر التشغيل والتقارير، وتنفّذ التعديلات بتأكيد.

البوت **ليس مساعداً ثانياً**. هو نفس المساعد الذكي الموجود في الموقع
(`lib/assistant`) بنفس المزوّد ونفس الأدوات ونفس بناء الخطة وتنفيذها — كل ما
يضيفه هو هوية وذاكرة وأزرار تأكيد.

---

## ما يقدر يسويه

**أسئلة** — يجاوب من بيانات النظام مباشرة:

- كم عدد المستفيدين النشطين؟
- وش منيو الغداء بكرة؟
- اعطني ملخص أمر التشغيل لليوم
- مين المستفيدين اللي عندهم حساسية سمك؟

**أوامر** — يعرض معاينة وزرّي «تأكيد» و«إلغاء» قبل أي تغيير، وزرّ «تراجع» بعده:

- أضف مستفيد اسمه فهد في غرفة ٢٠٤
- احذف السمك من وجبات محمد
- غيّر غداء الأحد إلى كبسة دجاج

**أوامر جاهزة:** `/help` `/new` `/whoami` `/unlink`

---

## الإعداد

الأداة `npm run telegram` تتكفّل بالباقي بعد أن تنشئ البوت.

```bash
npm run telegram                 # ما المضبوط وما الناقص
npm run telegram setup <token>   # تحقّق من الرمز واحفظه واربط كل شيء
npm run telegram migrate         # ترحيل قاعدة البيانات
npm run telegram dev             # شغّل البوت محلياً بلا نشر ولا ويب‑هوك
npm run telegram webhook <url>   # سجّل الويب‑هوك على نطاق منشور
npm run telegram webhook off     # ألغِ التسجيل
```

### ① أنشئ البوت

في تليقرام، افتح [@BotFather](https://t.me/BotFather) وأرسل `/newbot`. سينتهي
بإعطائك رمزاً بهذا الشكل:

```
8123456789:AAH_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### ② شغّل الترحيل

```bash
npm run telegram migrate
```

بلا رابط اتصال مباشر يطبع لك رابط محرّر SQL في Supabase لتلصق فيه
`supabase/telegram-migration.sql` — الترحيل (DDL) لا يمرّ عبر مفتاح الخدمة.
ومع رابط الاتصال يشغّله بنفسه:

```bash
SUPABASE_DB_URL='postgresql://…' npm run telegram migrate
```

يُنشئ خمسة جداول: الروابط، وأكواد الربط، وجلسات الحوار، والخطط المعلّقة،
وسجلّ التحديثات المعالَجة. كلها مغلقة أمام المتصفح (RLS بلا سياسات سماح).

### ③ فعّل الرمز

```bash
npm run telegram setup 8123456789:AAH_xxxxxxxxxxxxx
```

يتحقّق من الرمز عند تليقرام قبل حفظه، ويكتبه في `.env.local`، ويولّد
`TELEGRAM_WEBHOOK_SECRET` إن لم يكن موجوداً، ويسجّل قائمة الأوامر في تليقرام.

كذلك لازم يكون مضبوطاً من قبل:

- `SUPABASE_SERVICE_ROLE_KEY` — البوت يعمل بلا جلسة متصفح، فيحتاجه لقراءة
  الربط والصلاحيات. وهو نفسه مفتاح توقيع رموز التراجع.
- `GEMINI_API_KEY` أو `ANTHROPIC_API_KEY` — عقل المساعد. بدونهما لا يجاوب البوت.

### ④ وصّله بالخادم

**محلياً** (بلا نشر ولا عنوان عام) — نافذتان:

```bash
npm run dev            # الأولى
npm run telegram dev   # الثانية
```

الوضع الثاني يسحب التحديثات من تليقرام ويمرّرها لمسار الويب‑هوك المحلي بنفس
السرّ، فالمسار لا يعرف الفرق: ما تجرّبه محلياً هو ما سيعمل منشوراً.

> ابدأ الخادم **قبل** الجسر: `predev` يقتل ما يستمع على المنفذ ٣٠٠٠.

**منشوراً على Vercel:**

1. أضف في Settings ← Environment Variables: `TELEGRAM_BOT_TOKEN`،
   و`TELEGRAM_WEBHOOK_SECRET` (نفس القيمة التي في `.env.local`)،
   و`NEXT_PUBLIC_APP_URL` (نطاق النشر).
2. انشر، ثم:

```bash
npm run telegram webhook https://your-app.vercel.app
```

أو من الموقع: **الإعدادات ← بوت تليقرام ← تفعيل الويب‑هوك** (للأدمن).

## ربط المستخدمين

كل شخص يربط حسابه بنفسه:

1. الموقع ← **الإعدادات ← بوت تليقرام** ← **«إنشاء كود ربط»**
2. يفتح البوت في تليقرام ويرسل الكود

الكود ثمانية أحرف، صالح **١٥ دقيقة**، ويُستهلك **مرة واحدة**، وإنشاء كود جديد
يلغي القديم. الأدمن يقدر يفكّ ربط أي محادثة من الصفحة نفسها؛ وأي أحد يفكّ ربطه
بـ`/unlink` من داخل تليقرام.

**البوت يتكلم بصوت الحساب المربوط لا أكثر:** من ما يقدر يعدّل المستفيدين من
صفحتهم، ما يقدر يعدّلهم من تليقرام. ومن كانت تعديلاته تحتاج موافقة الأدمن،
يوجّهه البوت للموقع بدل الالتفاف حول مسار الموافقات. ومحادثة غير مربوطة ما
تشوف من النظام حرفاً.

---

## كيف يشتغل

```
تليقرام
   │  update (نص أو ضغطة زر)
   ▼
app/api/telegram/webhook   ← يتحقق من السرّ في الترويسة
   ▼
lib/telegram/handle        ← الهوية، الذاكرة، الأزرار
   ├── lib/telegram/store  ← الربط والجلسة والخطط المعلّقة
   └── lib/assistant/…     ← المحرّك نفسه الذي يخدم الموقع
         ├── ai/provider   ← Gemini أو Claude + الأدوات
         ├── plan.runTurn  ← بناء الخطة (حتمي، لا يثق بالنموذج)
         └── execute       ← التنفيذ + التقاط عكسه للتراجع
```

**ثلاثة قرارات تستحق الذكر:**

**التأكيد يعيد بناء الخطة.** الزر لا يحمل عمليات — يحمل معرّفاً لنصّ الأمر
وتوقيعه. وعند الضغط تُبنى الخطة من جديد على بيانات تلك اللحظة، ويُطابَق
التوقيع. لو تغيّرت البيانات بين المعاينة والتأكيد، يُرفض التنفيذ وتُطلب معاينة
جديدة — تماماً كما في الموقع.

**التحديث يُحجز قبل معالجته.** جولة النموذج قد تتجاوز مهلة تليقرام فيعيد
إرسال الرسالة نفسها؛ جدول `telegram_updates` يجعل أول من يحجز `update_id` هو
من ينفّذها. بدونه يُسأل النموذج مرتين وتُنفَّذ الرسالة مرتين.

**الحوار محفوظ في القاعدة.** المتصفح يحمل حواره ويعيد إرساله؛ تليقرام لا يفعل.
فنحفظه لكل محادثة، ونقصّه من الأقدم عند تجاوز الحد (القصّ من الأحدث يفصل نداء
الأداة عن نتيجته فيرفضه المزوّد)، وننساه بعد ساعتين خمول.

---

## حلّ المشاكل

| العرَض | السبب الغالب |
|---|---|
| البوت لا يرد إطلاقاً | الويب‑هوك غير مفعّل، أو `TELEGRAM_WEBHOOK_SECRET` مختلف بين تليقرام والخادم. راجع حالة الاتصال في صفحة الإعدادات. |
| «هذه المحادثة غير مربوطة» | الكود منتهٍ أو مستهلك — أنشئ غيره. |
| «المساعد غير مفعّل» | ينقص `GEMINI_API_KEY` أو `ANTHROPIC_API_KEY`. |
| «ما عندك صلاحية…» | صلاحيات الحساب المربوط. تُعدَّل من الإعدادات ← المستخدمون. |
| «تعديلاتك تحتاج موافقة الأدمن» | مقصود — نفّذ العملية من الموقع لتمرّ على مسار الموافقات. |
| البوت يرد ببطء شديد ثم يفشل | نموذج Gemini صامت لمفتاحك. الآن يُقطع بعد ٢٠ ثانية ويُسقَط للتالي تلقائياً، وتقدر تزيحه نهائياً: `GEMINI_MODEL=gemini-3.6-flash,gemini-3.5-flash-lite` |
| زر التراجع لا يعمل | صلاحية رمز التراجع ساعة واحدة، ثم ينتهي. |
| تفعيل الويب‑هوك يرفض localhost | تليقرام يقبل HTTPS فقط. استعمل نطاق النشر، أو نفقاً مثل ngrok للتجربة محلياً. |

**السجلات:** كل أخطاء البوت تُطبع بالبادئة `[telegram]` (على Vercel:
Deployments ← Functions ← Logs).
