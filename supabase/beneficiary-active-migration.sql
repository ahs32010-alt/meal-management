-- ============================================================================
-- Beneficiary Active (تعطيل مؤقت) Migration
-- يضيف عمود is_active للمستفيدين/المرافقين. عند false يُعتبر "معطّل مؤقتاً" (مسافر)
-- فلا يُحتسب في أوامر التشغيل ولا الستيكرات ولا التقارير حتى يُعاد تفعيله.
-- القيمة الافتراضية true (كل الموجودين حالياً مفعّلون).
-- ============================================================================

alter table public.beneficiaries
  add column if not exists is_active boolean not null default true;
